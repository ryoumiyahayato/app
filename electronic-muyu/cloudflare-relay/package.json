{
  "name": "electronic-muyu-cloudflare-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers + Durable Objects relay for electronic-muyu",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/protocol.test.js",
    "check": "npm test && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "test:integration": "node test/integration.mjs",
    "verify": "npm run check && npm run test:integration"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
